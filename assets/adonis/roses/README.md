# Roses

The real rose asset is not duplicated here — it already lives at:

- `public/rose/color/frame-001.webp` … `frame-080.webp`
- `public/rose/line/frame-001.webp` … `frame-080.webp`

160 files total. Copying them here would just create a second copy to keep
in sync for no benefit — reference the paths above directly. See
`.claude/skills/adonis-designer/BRAND_SYSTEM.md` → "The rose" for which
frame ranges read best in a static composition.
