# Backgrounds

No static file here — the one background texture in the brand system, the
low-opacity tiled "OMERTÀ" pattern, is generated by
`src/components/omerta-field.tsx`, not stored as an image. See
`.claude/skills/adonis-designer/BRAND_SYSTEM.md` → "The omertà pattern"
for how and when to use it in a static design.
